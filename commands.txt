git add- this is the command we use to tracked our file whic are created locally in our system.

git status- With this command we can check the status of the file whether this is tracked or not.

git commit- Once are files are added to stagging area, now we can commit our files with this command and now we can push to VCS.
hello new file
